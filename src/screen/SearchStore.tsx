import {
    HStack,
    Center,
    Pressable,
    Box,
    Heading,
    Text,
    Image,
    Input,
    Icon,
} from 'native-base';
import { Ionicons } from '@expo/vector-icons';
import { ImageSlider } from 'react-native-image-slider-banner';

export default function SeachStore({ navigation }) {

  return (
    <Box>
      
    </Box>
  )
}